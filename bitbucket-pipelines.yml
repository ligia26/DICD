image: atlassian/default-image:4

pipelines:
  default:
    - step:
        name: Test SSH connectivity
        script:
          - apt-get update && apt-get install -y openssh-client
          - ssh -vvv -o StrictHostKeyChecking=no root@149.102.137.255 "echo  Connected successfully!"
